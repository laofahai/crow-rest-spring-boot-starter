package ${packageName}.${appLC}.dto;

import lombok.Data;
import org.teamswift.crow.rest.common.ICrowDto;

@Data
public class ${module}Dto implements ICrowDto {

    // generator placeholder, don't remove this line.

}

